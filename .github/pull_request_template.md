## Summary

<!-- One sentence: what does this PR do? -->

Closes #

## Checklist

- [ ] This PR targets `main` and is rebased (or merged with latest `main`)
- [ ] One job only (does not mix unrelated features)
- [ ] No real secrets: `.env` is not committed; `.env.example` uses placeholders only
- [ ] New packages are listed in `BackEnd/requirements.txt` or `FrontEnd/package.json`
- [ ] I did not overwrite `BackEnd/main.py` / config with an older copy — I only added my lines
- [ ] No `__pycache__`, `node_modules`, or local db files

## How to test

<!-- Commands a reviewer can run, or "data-only, no runtime change" -->

## Notes for reviewers

<!-- Risks, follow-up work, or what should *not* merge next -->
